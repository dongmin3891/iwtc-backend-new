# IWTC 백엔드 NestJS 마이그레이션 계획

## 1. 문서 목적

이 문서는 기존 Spring Boot 백엔드를 직접 유지보수할지, Node.js 기반으로 전환할지 검토한 결과와 앞으로의 실행 방향을 기록한다.

현재 결론은 **기존 `prod` 소스를 동작 기준으로 보존하면서 NestJS 백엔드로 기능별 점진 전환**하는 것이다. 한 번에 전체를 다시 만드는 방식은 피하고, 프론트엔드가 사용 중인 API 계약과 기존 MySQL 데이터를 유지한 상태에서 교체한다.

## 2. 현재 백엔드 판단

### 브랜치 기준

- `prod`: 현재 운영 기준으로 삼을 브랜치
- `develop`: `prod`보다 이전 상태
- `jenkins-test`: `prod` 이후의 실험적인 멀티 모듈·Jenkins 작업이 포함된 브랜치

`jenkins-test`가 커밋 시점은 더 최신이지만, 배포 설정 변경이 대부분이고 구조와 운영 복잡도가 커졌다. 따라서 새 백엔드를 설계할 때 일부 모듈 경계만 참고하고, 기능과 API 동작의 기준은 `prod`로 삼는다.

### 기존 프로젝트의 주요 문제

- Spring Boot 3.1 계열이 지원 종료 상태이며 관련 의존성도 오래되었다.
- 설정 파일에 자격 증명으로 보이는 값이 커밋되어 있어 노출 여부 확인과 교체가 필요하다.
- Jenkins 설정에서 자격 증명을 출력하거나 Docker 빌드 인자로 전달할 가능성이 있다.
- 일부 MyBatis SQL이 `${...}` 문자열 치환을 사용해 안전성과 유지보수성이 떨어진다.
- 데이터베이스 마이그레이션 도구가 없어 스키마 변경 이력을 추적하기 어렵다.
- 홈서버에서 운영하기에는 멀티 서비스와 Jenkins 구성이 과도하다.

## 3. 기술 방향

### 선택

- 런타임: Node.js 24 LTS
- 프레임워크: NestJS 12
- 언어: TypeScript
- 데이터베이스: 기존 MySQL 유지
- ORM: Prisma
- 복잡한 랭킹·집계 쿼리: Prisma TypedSQL 또는 검증된 파라미터 바인딩 쿼리
- API: 기존 REST 경로와 응답 형식을 우선 유지하고 OpenAPI 문서화
- 캐시: Redis는 실제 필요성이 확인될 때만 유지 또는 도입
- 배포 단위: 단일 Docker 이미지의 모듈형 모놀리스

### NestJS를 선택하는 이유

- 프론트엔드와 같은 TypeScript 생태계를 사용해 전체 프로젝트를 혼자 관리하기 쉽다.
- 모듈, 컨트롤러, 서비스, DTO, 검증 구조가 정형화되어 일반 Node.js 서버보다 장기 유지보수에 유리하다.
- 현재 도메인 규모에서는 마이크로서비스보다 하나의 애플리케이션이 개발과 홈서버 운영 모두 단순하다.
- 기존 백엔드가 다른 개발자의 코드이므로, 단순 버전 업보다 새 구조를 직접 설계하며 소유권을 확보하는 가치가 있다.

## 4. 반드시 지킬 전환 원칙

1. 기존 Spring `prod`를 삭제하거나 즉시 대체하지 않는다.
2. 언어 전환과 데이터베이스 재설계를 동시에 하지 않는다.
3. 현재 MySQL 스키마와 데이터를 먼저 그대로 사용한다.
4. 프론트엔드가 의존하는 요청·응답·상태 코드를 계약으로 고정한다.
5. 기능 하나를 구현할 때마다 기존 서버와 결과를 비교한다.
6. 새 서버가 충분히 검증될 때까지 Spring 서버로 되돌릴 수 있게 유지한다.
7. 비밀값은 Git, Dockerfile, Docker 빌드 인자에 넣지 않는다.

## 5. 단계별 마이그레이션

### 0단계: 안전 조치

- 저장소에 포함된 자격 증명의 실제 사용 여부 확인
- DB, AWS/S3, JWT 등 노출 가능성이 있는 자격 증명 교체
- 운영 환경 변수 목록 작성
- 운영 DB 백업 및 복구 테스트

### 1단계: 기존 동작 고정

- Spring `prod` 로컬 실행 환경 구성
- 프론트엔드에서 사용하는 API 전체 목록 작성
- 요청 파라미터, 본문, 응답 JSON, 상태 코드, 인증 조건 기록
- 대표 데이터로 계약 테스트 또는 스냅샷 작성
- 기존 MySQL 스키마 덤프 및 관계 분석

### 2단계: NestJS 기반 구성

- 새로운 NestJS 프로젝트 생성
- 환경 변수 검증, 로깅, 예외 응답, 입력 검증, OpenAPI 설정
- Prisma로 기존 MySQL 스키마 introspection
- 도메인별 모듈 경계 정의
- Dockerfile과 로컬 Compose 구성

초기 모듈 후보:

- `world-cup`
- `game`
- `candidate`
- `rank`
- `reply`
- `member`
- `auth`
- `manage`
- `media`

### 3단계: 기능별 이전 순서

위험이 낮고 비교하기 쉬운 읽기 기능부터 이전한다.

1. 월드컵 공개 목록과 상세 조회
2. 후보 및 게임 데이터 조회
3. 게임 진행, 결과 저장, 클리어 처리
4. 랭킹과 통계
5. 댓글과 미디어 처리
6. 회원과 인증
7. 관리자 기능

각 기능은 다음 순서를 반복한다.

1. 기존 Spring 동작과 프론트엔드 사용 코드를 확인한다.
2. API 계약 테스트를 작성한다.
3. NestJS에 동일한 계약으로 구현한다.
4. 기존 DB를 대상으로 결과를 비교한다.
5. 프론트엔드 개발 환경에서 연결해 회귀 확인한다.

### 4단계: 전환

- 프론트엔드의 API 기본 주소를 NestJS 서버로 변경
- 초기에는 멤버 API와 일반 API 모두 같은 NestJS 주소 사용
- 홈서버에서 제한된 사용자 대상으로 검증
- 오류율, 응답 시간, DB 부하 확인
- 문제가 없으면 정식 전환
- 안정화 기간 동안 Spring 이미지를 롤백용으로 보관

## 6. 홈서버 배포 방향

권장 구성은 다음과 같다.

```text
Internet
   |
Caddy (HTTPS, reverse proxy)
   |
NestJS container
   |-- MySQL
   |-- Redis (필요한 경우)
   `-- S3-compatible object storage
```

- 외부에는 Caddy의 80/443 포트만 공개한다.
- MySQL과 Redis 포트는 인터넷에 공개하지 않는다.
- GitHub Actions에서 테스트와 이미지 빌드를 수행한다.
- 완성된 버전 태그 Docker 이미지를 GHCR에 저장한다.
- 홈서버는 이미지를 받아 실행하며 서버에서 직접 빌드하지 않는다.
- 컨테이너 health check와 restart policy를 설정한다.
- DB와 업로드 파일의 백업 사본 하나 이상을 홈서버 외부 위치에 보관한다.

## 7. 저장소 운영 방향

마이그레이션 구현은 기존 Spring 저장소 안에 섞기보다 별도 저장소에서 시작하는 것을 권장한다.

예시:

```text
iwtc-frontend2/       # 현재 프론트엔드
iwtc-backend2/        # 기존 Spring 기준·롤백 서버
iwtc-backend-nest/    # 신규 NestJS 서버
```

이 문서는 기존 백엔드 저장소에 보관해 전환의 배경과 기준을 남긴다. 실제 NestJS 코드가 시작되면 같은 문서를 신규 저장소로 복사하거나, 신규 저장소 README에서 이 문서를 기반으로 구현 현황을 관리한다.

## 8. 1차 완료 기준

- 프론트엔드가 사용하는 API 계약 목록이 완성되어 있다.
- 기존 운영 DB를 Prisma로 안전하게 읽을 수 있다.
- 주요 공개 조회 API가 기존 서버와 같은 결과를 반환한다.
- 테스트, Docker 이미지 빌드, 홈서버 배포가 자동화되어 있다.
- 자격 증명이 저장소와 이미지에 포함되지 않는다.
- DB 백업을 실제로 복구해 본 기록이 있다.
- Spring 서버로 되돌리는 절차가 문서화되어 있다.

## 9. 당장 진행할 작업

1. 기존 자격 증명 노출 범위 확인 및 교체
2. Spring `prod` 실행 환경 마련
3. 프론트엔드 기준 API 계약 목록 작성
4. 신규 `iwtc-backend-nest` 저장소 생성
5. NestJS 기본 구조와 Prisma introspection 구성

